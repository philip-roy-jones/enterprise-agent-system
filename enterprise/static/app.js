const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const submitted = new Set();
let active=localStorage.getItem('active-job'), selectedApproval=null, current=null, source=null, lastImage='', refreshing=false;
function toast(text){$('toast').textContent=text;$('toast').hidden=false;setTimeout(()=>$('toast').hidden=true,5000)}
async function api(path,body){const r=await fetch(path,{method:body===undefined?'GET':'POST',headers:{'Content-Type':'application/json'},...(body===undefined?{}:{body:JSON.stringify(body)})});if(r.status===401){if(!$('login-dialog').open)$('login-dialog').showModal();throw Error('Connect to your workspace to continue.')}const data=await r.json();if(!r.ok)throw Error(typeof data.detail==='object'?data.detail.message:JSON.stringify(data.detail));return data;}
async function action(fn){try{await fn();await refresh()}catch(e){toast(e.message)}}
$('login-form').onsubmit=e=>{e.preventDefault();action(async()=>{await api('/api/session',{token:$('token').value});$('login-dialog').close()})};
for(const id of ['new-job','first-job'])$(id).onclick=()=>{$('mode').value='strict';$('job-dialog').showModal()};$('close-dialog').onclick=()=>$('job-dialog').close();
const scenarios={normal:{view:'dashboard',dialog:null,variant:'standard',reordered:false,interrupt_save:false,unsaved:false},wrong:{view:'invoice',invoice_id:'INV-1044'},reordered:{view:'invoices',reordered:true},delayed:{delay_seconds:4},info:{dialog:'info'},unsaved:{view:'invoice',invoice_id:'INV-1044',dialog:'unsaved',unsaved:true},unfamiliar:{dialog:'unfamiliar'},renamed:{variant:'renamed'},layout:{variant:'layout'},ambiguous:{interrupt_save:true}};
$('job-form').onsubmit=e=>{e.preventDefault();action(async()=>{await api('/api/mock/scenario',{...scenarios.normal,...scenarios[$('scenario').value]});const j=await api('/api/jobs',{invoice_id:$('invoice').value,selected_mode:$('mode').value});active=j.id;localStorage.setItem('active-job',active);$('job-dialog').close();connectStream()})};
function connectStream(){source?.close();if(!active)return;source=new EventSource(`/api/jobs/${active}/stream`);source.onmessage=()=>refresh();}
function showJobs(jobs){$('job-count').textContent=jobs.length;$('job-list').innerHTML=jobs.length?jobs.map(j=>`<button class="job-row ${j.id===active?'selected':''}" data-job="${j.id}"><span class="job-row-top"><strong>${esc(j.invoice_id)}</strong><span class="status-dot ${esc(j.status)}"></span></span><span>${esc(j.status.replaceAll('_',' '))}</span><small>${j.selected_mode==='auto'?'Auto':'Strict'} · ${esc(j.graph_version)} · ${new Date(j.created_at*1000).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small></button>`).join(''):'<p class="empty-queue">No jobs yet</p>';document.querySelectorAll('[data-job]').forEach(el=>el.onclick=()=>{active=el.dataset.job;localStorage.setItem('active-job',active);selectedApproval=null;connectStream();refresh()});}
const readable={validate:'Validate',establish:'Open invoice',compare:'Compare PO',prepare:'Prepare draft',save:'Save',verify:'Verify',complete:'Complete'};
function renderDetail(d){current=d;const j=d.job, pending=d.approvals.find(a=>a.status==='pending'&&!submitted.has(a.id)), done=['completed','cancelled','rejected','failed','denied'].includes(j.status);$('empty').hidden=true;$('active-job').hidden=false;$('job-id').textContent=`${j.graph_version} / ${j.id.slice(0,8)}`;$('job-title').textContent=`Correct ${j.invoice_id}`;$('job-status').textContent=j.status;$('job-mode').value=j.pending_mode||j.selected_mode;$('job-mode').disabled=done;$('cancel-job').disabled=done;$('takeover').disabled=done;$('takeover').textContent=d.lease.owner==='staff'?'Release control':'Take control';$('accept-job').hidden=j.status!=='completed'||j.accepted;$('control-label').textContent=done?'Available':({script:'Scripted worker',assistant:'Supervised assistant',staff:'Staff'}[d.lease.owner]||'Waiting');$('control-detail').textContent=done?'Ready for the next job':`Exclusive session · lease ${d.lease.epoch}`;$('mode-label').textContent=j.effective_mode==='strict'?'Strict':'Auto';$('mode-detail').textContent=j.controller==='assistant'?'Strict — staff approval required during assistance.':j.effective_mode==='strict'?'Staff approval before each operation':'Procedures run with scoped permissions';$('job-error').textContent=j.error||'';$('progress').innerHTML=Object.entries(readable).map(([id,label],i)=>`<div class="step ${j.completed.includes(id)?'finished':''}"><span>${j.completed.includes(id)?'✓':i+1}</span><small>${label}</small></div>`).join('');$('episode-link').href=`/api/jobs/${j.id}/episode`;
const latestObs=[...d.events].reverse().find(e=>e.kind==='observation')?.data;let obs=pending?.observation||latestObs;if(obs?.screenshot&&obs.screenshot!==lastImage){$('screenshot').src='/api/artifacts/'+obs.screenshot;lastImage=obs.screenshot;}$('screenshot-wrap').hidden=!obs;$('review-controls').hidden=!pending;$('waiting').hidden=!!pending;$('waiting').textContent=done?(j.accepted?'Staff accepted the verified result.':j.status==='completed'?'Correction draft verified. Accept the work to make this episode available for improvement.':`Job ${j.status}.`):d.lease.owner==='staff'?'You hold desktop control. Use the accounting workspace, then release control.':'Worker is running or reconciling current state.';
if(pending){$('review-title').textContent=pending.kind==='tool'?'Assistance needs your decision':'Review next operation';$('review-kind').textContent=pending.kind==='tool'?'STRICT · TOOL APPROVAL':'NODE APPROVAL';$('proposal-copy').innerHTML=`<h3>${esc(pending.name.replaceAll('_',' '))}</h3><p>${esc(pending.description)}</p><div class="proposal-meta"><span>Target<strong>${esc(pending.inputs.company_id)} / ${esc(pending.inputs.invoice_id)}</strong></span><span>Expected result<strong>${esc(pending.expected)}</strong></span></div>`;
if(selectedApproval?.id!==pending.id){selectedApproval=pending;$('arguments').value=JSON.stringify(pending.arguments,null,2);$('decision-note').value='';drawTarget(pending.arguments,obs)}$('arguments').readOnly=pending.kind!=='tool';$('correct').hidden=pending.kind!=='tool';$('approve').textContent=pending.kind==='tool'?'Approve tool call':'Approve operation';}else{selectedApproval=null;$('review-title').textContent='Live workspace';$('review-kind').textContent=j.model_mode==='simulated'?'SIMULATED ASSISTANCE':'LIVE MODEL';$('proposal-copy').innerHTML='';$('target-circle').hidden=true;}
const visible=d.events.filter(e=>!['observation','action_started','action_result'].includes(e.kind));$('timeline').innerHTML=visible.slice(-35).reverse().map(e=>{let text=e.data.text||e.data.reason||e.data.message||(e.kind==='progress'?`${readable[e.data.operation]||e.data.operation} finished`:'');if(e.kind==='approval_requested')text=e.data.description;if(e.kind==='staff_decision')text=`${e.data.decision.decision}: ${e.data.name}${e.data.decision.explanation?' — '+e.data.decision.explanation:''}`;return `<div class="timeline-item"><span class="timeline-mark"></span><div><strong>${esc(e.kind.replaceAll('_',' '))}</strong><p>${esc(text)}</p></div><time>${new Date(e.at*1000).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'})}</time></div>`}).join('');}
function drawTarget(args,obs){let t=obs.targets.find(t=>t.target===args.target);if(args.x!=null)t=args;if(!t){$('target-circle').hidden=true;return}$('target-circle').hidden=false;$('target-circle').style.left=(t.x/obs.width*100)+'%';$('target-circle').style.top=(t.y/obs.height*100)+'%'}
$('screenshot').onclick=e=>{if(selectedApproval?.name!=='click')return;const r=e.target.getBoundingClientRect(),obs=selectedApproval.observation,args={x:Math.round((e.clientX-r.left)/r.width*obs.width),y:Math.round((e.clientY-r.top)/r.height*obs.height)};$('arguments').value=JSON.stringify(args,null,2);drawTarget(args,obs)};
async function decide(decision){if(!selectedApproval)return;const id=selectedApproval.id;for(const b of ['approve','correct','reject'])$(b).disabled=true;try{await api(`/api/approvals/${id}`,{decision,explanation:$('decision-note').value,...(decision==='correct'?{arguments:JSON.parse($('arguments').value)}:{})});submitted.add(id);selectedApproval=null;$('review-controls').hidden=true;}finally{for(const b of ['approve','correct','reject'])$(b).disabled=false}}
$('approve').onclick=()=>action(()=>decide('approve'));$('correct').onclick=()=>action(()=>decide('correct'));$('reject').onclick=()=>action(()=>decide('reject'));$('job-mode').onchange=()=>action(()=>api(`/api/jobs/${active}/mode`,{mode:$('job-mode').value}));$('cancel-job').onclick=()=>action(()=>api(`/api/jobs/${active}/cancel`,{}));$('accept-job').onclick=()=>action(()=>api(`/api/jobs/${active}/accept`,{}));$('takeover').onclick=()=>action(()=>api(`/api/jobs/${active}/${current.lease.owner==='staff'?'release':'takeover'}`,{}));$('message-form').onsubmit=e=>{e.preventDefault();action(async()=>{await api(`/api/jobs/${active}/messages`,{text:$('message').value});$('message').value=''})};
$('metrics-tab').onclick=()=>action(async()=>{$('main-view').hidden=true;$('metrics-view').hidden=false;const m=await api('/api/metrics');$('metrics-view').innerHTML=`<section class="panel"><h2>Evaluation metrics</h2><p class="muted">Simulated harness runs and live-model runs are reported separately.</p><table><thead><tr><th>Metric</th><th>Simulated</th><th>Live</th></tr></thead><tbody>${Object.keys(m.simulated).map(k=>`<tr><td>${esc(k.replaceAll('_',' '))}</td><td>${m.simulated[k]??'—'}</td><td>${m.live[k]??'—'}</td></tr>`).join('')}</tbody></table></section>`});$('jobs-tab').onclick=()=>{$('main-view').hidden=false;$('metrics-view').hidden=true};
async function refresh(){if(refreshing)return;refreshing=true;try{const jobs=await api('/api/jobs');if(!active&&jobs.length){active=jobs[0].id;connectStream()}showJobs(jobs);if(active&&jobs.some(j=>j.id===active))renderDetail(await api('/api/jobs/'+active));const health=await api('/api/health');$('model-label').textContent=health.model_mode==='simulated'?'Simulated assistance':'Live model';}catch(e){if(!$('login-dialog').open)toast(e.message)}finally{refreshing=false}}
refresh();connectStream();setInterval(refresh,2500);
